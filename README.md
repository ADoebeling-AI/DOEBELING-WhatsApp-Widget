# Screenshots

This branch only holds screenshots for pull requests. It has no common history with `main` and is never merged.
One folder per topic; existing images are not replaced, new versions get a suffix like `-v2`.
