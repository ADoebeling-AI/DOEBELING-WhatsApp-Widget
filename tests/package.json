{
  "name": "whatsapp-widget-tests",
  "private": true,
  "description": "Browser and HTTP tests for the WhatsApp widget, the PHP script and the notification service",
  "scripts": {
    "test": "node run.mjs"
  },
  "devDependencies": {
    "playwright": "1.56.1"
  }
}
